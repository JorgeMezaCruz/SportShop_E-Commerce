@model E_Commerce20.Models.Ciudades

@{
    ViewBag.Title = "CiudadesMntCreate";
    Layout = "~/Views/Shared/_LayoutCompleto.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <br />
    <div class="card shadow-lg">
        <div class="card-header">
            <h6 class="m-0 font-weight-bold text-dark">Registro de ciudades</h6>
        </div>
        <div class="card-body">
            <form class="user">
                @{
                    if (!string.IsNullOrEmpty(ViewBag.mensaje))
                    {
                        <div class="alert alert-success">@ViewBag.mensaje</div>
                    }
                }

                <div class="form-group row">
                    <div class="col-sm-6 mb-3 mb-sm-0">
                        @Html.LabelFor(model => model.id_ciudad, htmlAttributes: new { @class = "control-label col-md-6" })
                        @Html.EditorFor(model => model.id_ciudad, new { htmlAttributes = new { @class = "form-control form-control-user" } })
                        @Html.ValidationMessageFor(model => model.id_ciudad, "", new { @class = "text-danger" })

                    </div>
                    <div class="col-sm-6">
                        @Html.LabelFor(model => model.nombre_ciudad, htmlAttributes: new { @class = "control-label col-md-6" })
                        @Html.EditorFor(model => model.nombre_ciudad, new { htmlAttributes = new { @class = "form-control form-control-user" } })
                        @Html.ValidationMessageFor(model => model.nombre_ciudad, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Registrar" class="btn btn-primary btn-user btn-block" />
                </div>
            </form>
        </div>
    </div>
}

<br />
<a href="@Url.Action("CiudadesMntIndex", "CiudadesMnt")" class="btn btn-light btn-icon-split">
    <span class="icon text-dark">
        <i class="fas fa-angle-double-left "></i>
    </span>
    <span class="text">Regresar al Listado</span>
</a>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
